{
  "scripts": {
    "start": "concurrently \"npx json-server database.json\" \"cd peak-pages && npm start\""
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}